A library that makes LOOP simple again.

examples:

(@:loop
  <code>
  (@:exit-when <condition>))

(@:loop
  <code>
  (@:exit-when <condition>))

(@:loop
  <code>
  (@:exit-when <condition>)
  <code>)





