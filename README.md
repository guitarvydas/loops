A library that makes LOOP simple again.

examples:

(@:loop
  <code>
  (@:exit-when _condition_))

(@:loop
  <code>
  (@:exit-when _condition_))

(@:loop
  <code>
  (@:exit-when _condition_)
  <code>)





